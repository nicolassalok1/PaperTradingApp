# Intentionally empty: this package used to re-export controller_bridge with a
# star import, which created the app.vue.components.options <-> controller_bridge
# cycle. No module ever imported a symbol from the package itself.
