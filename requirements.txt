############################################################
# Streamlit Cloud (lean) requirements
# - Keep this file minimal to reduce build time and avoid heavy deps.
# - See `requirements_full.txt` for the full dev stack.
############################################################

streamlit>=1.51.0,<2

numpy>=1.26,<3
pandas>=2.1,<3
requests>=2.31,<3
python-dotenv>=1.0,<2

altair>=5.3,<6
plotly>=5.22,<6
matplotlib>=3.8,<4
scipy>=1.11,<2

alpaca-py>=0.12,<1

# Optional (the app degrades gracefully if missing)
openai>=1.13,<2
