############################################################
# Full (dev) requirements used locally.
#
# Dependencies are now split by purpose under requirements/:
#   runtime.txt     - run the app (degrades gracefully without ML/AI extras)
#   runtime-ml.txt  - optional ML inference (Heston NN: torch)
#   test.txt        - CI test stack (runtime + pytest, NO torch)
#   train.txt       - offline Heston-net training
#   dev.txt         - everything (runtime + ml + test + QA tooling)
#   lock.txt        - full pinned snapshot of the known-good env
#
# Streamlit Cloud should keep using `requirements.txt` (lean).
# Removed as unused (zero imports, verified): tensorflow, scikit-learn,
# statsmodels, seaborn, wordcloud, websockets.
############################################################
-r requirements/dev.txt
